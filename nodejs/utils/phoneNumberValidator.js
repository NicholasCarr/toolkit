import { parsePhoneNumber } from 'google-libphonenumber';

// TODO - REVIEW THE LIBRARY AND TEST
async function validatePhoneNumber(phone, country_region_code, client) {
//   if (!phone) return null;
//   // Remove any non-digit characters, remove whitespace
//   const digits = phone.replace(/\D/g, '').replace(/\s/g, '');
  
  
//   // Get country code from contact, or use client config, default to +1 if not set
//   const countryCode = country_region_code || client?.phone?.country_code || '+1';
  
//   // If number already has country code, return as is
//   if (digits.startsWith(countryCode.replace('+', ''))) {
//       return `+${digits}`;
//   }
  
//   // Add country code if not present
//   return `${countryCode}${digits}`;



}